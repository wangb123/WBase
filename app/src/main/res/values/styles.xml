<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="WBaseTranslucent">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_control</item>
        <item name="colorPrimaryDark">@color/color_brand</item>
        <item name="colorAccent">@color/color_control</item>
    </style>

</resources>
